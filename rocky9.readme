dbf update
dnf -y install "epel-release"
sudo yum install groupinstall "Development Tools"
Type:python build_xspress.py extrct configure

Type:sudo make(base)
Error:Can't locate FindBin.pm in @INC (you may need to install the FindBin module)
sudo install perl

EPICS_HOST_ARCH
so:sudo -y yum install perl && make


Error:rpcgen not found or rpc.h
sudo yum install rpcgen  &&   dnf install glibc-utils
 libtirpc, you will also have to uncomment this line in configure/CONFIG_SITE: # TIRPC=YES
